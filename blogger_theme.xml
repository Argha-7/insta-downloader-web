<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:css='false' b:defaultwidgetversion='2' b:layoutsversion='3'
    xmlns='http://www.w3.org/1999/xhtml'
    xmlns:b='http://www.google.com/gastrouts'
    xmlns:data='http://www.google.com/schema/data'
    xmlns:expr='http://www.google.com/gastrouts/expressions'>
    <head>
        <meta charset='UTF-8'/>
        <meta content='width=device-width, initial-scale=1.0' name='viewport'/>
        <title>
            <data:blog.pageTitle/>
        </title>

        <link href='https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700;800&amp;display=swap' rel='stylesheet'/>
        <link href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css' rel='stylesheet'/>
        <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

        <b:skin><![CDATA[ /* Blogger Required Skin */ ]]></b:skin>

        <style>
            :root {
                --primary: #3b82f6; /* More vibrant blue like image */
                --primary-dark: #2563eb;
                --secondary: #06b6d4;
                --dark: #0f172a;
                --light: #f8fafc;
                --white: #ffffff;
                --text-muted: #64748b;
                --border: #e2e8f0;
                --glass: rgba(255, 255, 255, 0.6);
                --glass-border: rgba(255, 255, 255, 0.5);
                --shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.07);
                --grad: linear-gradient(135deg, #3b82f6 0%, #2dd4bf 100%);
            }

            * { box-sizing: border-box; transition: all 0.3s ease; }

            html { background-color: var(--light); }
            body {
                margin: 0;
                background-color: transparent;
                color: var(--dark);
                font-family: 'Outfit', sans-serif;
                overflow-x: hidden;
            }

            /* Advanced Animations */
            @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
            @keyframes pulse { 0% { opacity: 0.6; } 50% { opacity: 1; } 100% { opacity: 0.6; } }
            @keyframes shimmer { 0% { background-position: -200% 0; } 100% { background-position: 200% 0; } }
            @keyframes reveal { 0% { width: 0; } 100% { width: 100%; } }
            @keyframes glow { 0%, 100% { box-shadow: 0 0 15px rgba(59, 130, 246, 0.3); } 50% { box-shadow: 0 0 30px rgba(59, 130, 246, 0.6); } }
            @keyframes igGlow { 0% { border-color: #f09433; box-shadow: 0 0 10px #f09433; } 33% { border-color: #e6683c; box-shadow: 0 0 10px #e6683c; } 66% { border-color: #dc2743; box-shadow: 0 0 10px #dc2743; } 100% { border-color: #cc2366; box-shadow: 0 0 10px #cc2366; } }

            .animate { animation: fadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards; }
            .delay-1 { animation-delay: 0.2s; }
            .delay-2 { animation-delay: 0.4s; }

            /* Ad Placements */
            .ad-slot {
                width: 100%;
                max-width: 728px;
                min-height: 90px;
                margin: 20px auto;
                background: rgba(0,0,0,0.03);
                border: 1px dashed #cbd5e1;
                border-radius: 12px;
                display: flex;
                align-items: center;
                justify-content: center;
                color: #94a3b8;
                font-size: 0.8rem;
                overflow: hidden;
            }

            /* Navbar */
            .navbar {
                background: var(--glass);
                backdrop-filter: blur(15px);
                padding: 1.2rem 10%;
                display: flex;
                justify-content: space-between;
                align-items: center;
                border-bottom: 1px solid var(--glass-border);
                position: sticky;
                top: 0;
                z-index: 1000;
                box-shadow: 0 4px 30px rgba(0, 0, 0, 0.03);
            }

            .nav-links { display: flex; gap: 35px; align-items: center; }
            .nav-links a { 
                text-decoration: none; 
                color: #475569; 
                font-weight: 600; 
                font-size: 0.95rem;
                transition: 0.3s;
            }
            .nav-links a:hover { color: var(--primary); }

            .logo {
                font-size: 1.6rem;
                font-weight: 700;
                color: var(--primary);
                text-decoration: none;
                display: flex;
                align-items: center;
                gap: 12px;
            }

            .btn-started {
                background: linear-gradient(90deg, #3b82f6, #2dd4bf, #3b82f6);
                background-size: 200% auto;
                color: white;
                padding: 12px 28px;
                border-radius: 50px;
                text-decoration: none;
                font-weight: 600;
                box-shadow: 0 10px 20px rgba(59, 130, 246, 0.3);
                transition: 0.5s;
                animation: shimmer 3s linear infinite;
            }

            .btn-started:hover { transform: translateY(-3px); box-shadow: 0 15px 30px rgba(99, 102, 241, 0.4); }

            .nav-wallet {
                background: rgba(59, 130, 246, 0.1);
                padding: 8px 15px;
                border-radius: 50px;
                display: flex;
                align-items: center;
                gap: 8px;
                color: var(--primary);
                font-weight: 700;
                border: 1px solid rgba(59, 130, 246, 0.2);
                transition: all 0.3s ease;
            }
            .nav-wallet i { color: #facc15; }
            .nav-wallet:hover { transform: translateY(-2px); box-shadow: 0 5px 15px rgba(59,130,246,0.1); cursor: pointer; }
            
            .nav-cash {
                background: rgba(34, 197, 94, 0.1);
                border-color: rgba(34, 197, 94, 0.2);
                color: #15803d;
            }
            .nav-cash i { color: #22c55e !important; }

            /* Earnings Dashboard */
            .dashboard-modal {
                display: none;
                position: fixed;
                top: 0; left: 0; width: 100%; height: 100%;
                background: rgba(0,0,0,0.6);
                backdrop-filter: blur(10px);
                z-index: 2000;
                justify-content: center;
                align-items: center;
            }
            .dash-content {
                background: white;
                width: 90%;
                max-width: 500px;
                padding: 40px;
                border-radius: 30px;
                position: relative;
                text-align: center;
                box-shadow: 0 25px 50px -12px rgba(0,0,0,0.25);
            }
            .dash-close { position: absolute; top: 20px; right: 20px; font-size: 1.5rem; cursor: pointer; color: #94a3b8; }
            .dash-balance { font-size: 2.8rem; font-weight: 800; color: var(--dark); margin: 15px 0; }
            .ref-box {
                background: #f1f5f9;
                padding: 12px 15px;
                border-radius: 12px;
                display: flex;
                gap: 10px;
                margin: 20px 0;
                border: 1px solid #e2e8f0;
            }
            .ref-box input { border: none; background: transparent; flex: 1; font-weight: 600; color: #475569; font-size: 0.9rem; }
            .btn-withdraw {
                width: 100%;
                background: #1e293b;
                color: white;
                padding: 15px;
                border-radius: 12px;
                border: none;
                font-weight: 700;
                margin-top: 10px;
                cursor: pointer;
                transition: 0.3s;
            }
            .btn-withdraw:hover { background: #0f172a; transform: scale(1.02); }
            

            .btn-whatsapp {
                display: inline-flex;
                align-items: center;
                gap: 10px;
                background: #25D366;
                color: white;
                padding: 12px 25px;
                border-radius: 50px;
                text-decoration: none;
                font-weight: 600;
                transition: 0.3s;
                margin-top: 10px;
            }
            .btn-whatsapp:hover { background: #128C7E; transform: scale(1.05); }

            /* Hero Section */
            .hero {
                padding: 100px 20px 140px;
                text-align: center;
                position: relative;
                overflow: hidden;
            }

            .hero::before {
                content: '';
                position: absolute;
                top: -10%; right: -10%;
                width: 400px; height: 400px;
                background: radial-gradient(circle, rgba(59, 130, 246, 0.1) 0%, transparent 70%);
                filter: blur(80px);
                z-index: -1;
                animation: float 10s infinite alternate;
            }

            .hero::after {
                content: '';
                position: absolute;
                bottom: -10%; left: -10%;
                width: 350px; height: 350px;
                background: radial-gradient(circle, rgba(45, 212, 191, 0.1) 0%, transparent 70%);
                filter: blur(80px);
                z-index: -1;
                animation: float 12s infinite alternate-reverse;
            }

            .hero h1 {
                font-size: clamp(2.8rem, 8vw, 4.5rem);
                font-weight: 800;
                margin-bottom: 25px;
                line-height: 1.1;
                color: #1e40af; /* Deep blue like the image */
                background: linear-gradient(to bottom, #1e40af, #3b82f6);
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                letter-spacing: -3px;
            }

            .hero p { color: var(--text-muted); font-size: 1.25rem; max-width: 700px; margin: 0 auto 50px; }

            /* Search Box (Glassmorphism) */
            .search-card {
                max-width: 900px;
                margin: 0 auto;
                background: var(--glass);
                backdrop-filter: blur(20px);
                padding: 15px;
                border-radius: 24px;
                display: flex;
                border: 2px solid transparent;
                box-shadow: var(--shadow);
                transition: 0.4s;
                position: relative;
                z-index: 10;
            }

            .search-card:focus-within { 
                animation: igGlow 3s linear infinite;
            }

            .typing-cursor {
                display: inline-block;
                width: 3px;
                height: 1em;
                background: var(--primary);
                margin-left: 5px;
                vertical-align: middle;
                animation: pulse 1s infinite;
            }

            #particle-canvas {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                z-index: -1;
                pointer-events: none;
            }

            /* --- Custom Aura Cursor --- */
            .cursor-point {
                width: 6px;
                height: 6px;
                background: var(--primary);
                border-radius: 50%;
                position: fixed;
                pointer-events: none;
                z-index: 10000;
                transition: transform 0.1s;
            }

            .cursor-aura {
                width: 40px;
                height: 40px;
                background: rgba(59, 130, 246, 0.2);
                border: 2px solid rgba(59, 130, 246, 0.4);
                border-radius: 50%;
                position: fixed;
                pointer-events: none;
                z-index: 9999;
                transition: width 0.3s, height 0.3s, background 0.3s, border 0.3s;
                transform: translate(-50%, -50%);
            }

            .cursor-aura.hover {
                width: 80px;
                height: 80px;
                background: rgba(59, 130, 246, 0.1);
                border: 1px solid var(--primary);
            }

            @media (max-width: 1024px) {
                .cursor-point, .cursor-aura { display: none; }
            }

            .search-card input {
                flex: 1;
                border: none;
                padding: 20px 35px;
                font-size: 1.2rem;
                font-family: inherit;
                outline: none;
                background: transparent;
            }

            .limit-badge {
                display: inline-flex;
                align-items: center;
                gap: 8px;
                padding: 6px 15px;
                background: rgba(99, 102, 241, 0.1);
                color: var(--primary);
                border-radius: 50px;
                font-size: 0.85rem;
                font-weight: 700;
                margin-bottom: 20px;
                border: 1px solid rgba(99, 102, 241, 0.2);
            }

            .modal {
                display: none;
                position: fixed;
                top: 0; left: 0; width: 100%; height: 100%;
                background: rgba(0,0,0,0.6);
                z-index: 9999;
                backdrop-filter: blur(5px);
                align-items: center;
                justify-content: center;
            }

            .modal-content {
                background: white;
                padding: 40px;
                border-radius: 24px;
                max-width: 400px;
                width: 90%;
                text-align: center;
                animation: fadeIn 0.3s ease;
            }

            .btn-action {
                background: var(--dark);
                color: white;
                border: none;
                padding: 0 40px;
                border-radius: 18px;
                font-weight: 600;
                font-size: 1.1rem;
                cursor: pointer;
            }

            .btn-action:hover { background: var(--primary); }

            /* Pricing Section */
            .pricing {
                padding: 100px 10%;
                text-align: center;
                background: var(--light);
            }

            .pricing-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
                gap: 30px;
                margin-top: 50px;
            }

            .p-card {
                background: var(--glass);
                backdrop-filter: blur(15px);
                padding: 40px;
                border-radius: 24px;
                border: 1px solid var(--glass-border);
                position: relative;
                overflow: hidden;
                box-shadow: var(--shadow);
            }

            .p-card.popular {
                border: 2px solid var(--primary);
                transform: scale(1.05);
                box-shadow: var(--shadow);
            }

            .p-card h4 { font-size: 1.2rem; margin-bottom: 10px; color: var(--primary); }
            .p-card .price { font-size: 2.5rem; font-weight: 800; margin-bottom: 20px; }
            .p-card .price span { font-size: 1rem; color: #64748b; }
            .p-card ul { list-style: none; padding: 0; margin: 30px 0; text-align: left; }
            .p-card ul li { margin-bottom: 12px; color: #64748b; display: flex; align-items: center; gap: 10px; }
            .p-card ul li i { color: #22c55e; }

            .btn-outline {
                display: block;
                padding: 12px;
                border: 2px solid var(--primary);
                color: var(--primary);
                text-decoration: none;
                border-radius: 50px;
                font-weight: 600;
            }

            .btn-outline:hover { background: var(--primary); color: white; }

            /* Progress UI */
            .progress-card {
                max-width: 600px;
                margin: 40px auto;
                background: var(--glass);
                backdrop-filter: blur(15px);
                padding: 30px;
                border-radius: 24px;
                box-shadow: var(--shadow);
                display: none;
                flex-direction: column;
                gap: 20px;
                text-align: left;
                border: 1px solid var(--glass-border);
            }

            .progress-header { display: flex; gap: 20px; align-items: center; margin-bottom: 10px; }
            .prog-thumb { width: 80px; height: 80px; border-radius: 12px; object-fit: cover; background: #eee; }
            .prog-info { flex: 1; }
            .prog-title { font-weight: 700; color: var(--dark); font-size: 1rem; }

            .progress-bar-wrap { width: 100%; background: #f1f5f9; border-radius: 10px; height: 12px; overflow: hidden; }
            #progressBar { width: 20%; height: 100%; background: var(--grad); transition: 0.5s cubic-bezier(0.1, 0.7, 0.1, 1); }
            #statusText { font-weight: 600; font-size: 1rem; color: var(--primary); }

            /* Result Card Redesign */
            #result {
                display: none;
                margin-top: 50px;
                max-width: 650px;
                margin-left: auto;
                margin-right: auto;
            }

            .result-card {
                background: var(--glass);
                backdrop-filter: blur(20px);
                border-radius: 24px;
                padding: 35px;
                box-shadow: var(--shadow);
                display: flex;
                gap: 30px;
                text-align: left;
                align-items: center;
                border: 1px solid var(--glass-border);
                margin-bottom: 20px;
            }

            .res-thumb {
                width: 140px;
                height: 140px;
                border-radius: 8px;
                object-fit: cover;
                background: #f1f5f9;
            }

            .res-info { flex: 1; }
            .res-title { font-size: 1.25rem; font-weight: 700; color: #0f172a; margin-bottom: 5px; text-transform: uppercase; }
            .res-meta { font-size: 0.9rem; color: #64748b; margin-bottom: 15px; }

            .btn-download-final {
                background: #10b981;
                color: white;
                text-decoration: none;
                padding: 12px 25px;
                border-radius: 8px;
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 10px;
                font-weight: 600;
                width: 100%;
                margin-bottom: 12px;
                transition: 0.3s;
            }
            .btn-download-final:hover { background: #059669; }

            .btn-copy-link {
                background: white;
                color: #6366f1;
                border: 2px solid #6366f1;
                padding: 10px;
                border-radius: 8px;
                width: 100%;
                font-weight: 600;
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 10px;
                cursor: pointer;
                transition: 0.3s;
            }
            .btn-copy-link:hover { background: #f5f3ff; }

            .success-banner {
                background: #d1fae5;
                color: #065f46;
                padding: 15px;
                border-radius: 8px;
                font-size: 0.95rem;
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 10px;
                font-weight: 500;
            }

            /* Features Section */
            .features {
                padding: 100px 10%;
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 40px;
                background: radial-gradient(circle at bottom right, rgba(99, 102, 241, 0.03), transparent);
            }

            .f-card {
                padding: 40px;
                border-radius: 24px;
                background: var(--glass);
                backdrop-filter: blur(10px);
                border: 1px solid var(--glass-border);
                transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
                box-shadow: var(--shadow);
            }

            .f-card:hover { 
                transform: translateY(-20px); 
                background: white; 
                box-shadow: 0 30px 60px rgba(0,0,0,0.1); 
            }

            .f-card i { font-size: 2.5rem; margin-bottom: 20px; color: var(--primary); }
            .f-card h3 { font-size: 1.5rem; margin-bottom: 15px; }
            .f-card p { color: #64748b; line-height: 1.7; }

            /* Footer Upgrade */
            footer {
                padding: 80px 10% 40px;
                background: #0f172a;
                color: #e2e8f0;
                text-align: left;
                margin-top: 50px;
            }

            .footer-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                gap: 50px;
                border-bottom: 1px solid rgba(255,255,255,0.1);
                padding-bottom: 60px;
                margin-bottom: 40px;
            }

            .footer-col h4 {
                color: white;
                font-size: 1.1rem;
                margin-bottom: 25px;
                font-weight: 700;
            }

            .footer-col ul { list-style: none; padding: 0; margin: 0; }
            .footer-col ul li { margin-bottom: 15px; }
            .footer-col ul li a {
                color: #94a3b8;
                text-decoration: none;
                font-size: 0.95rem;
                transition: 0.3s;
            }
            .footer-col ul li a:hover { color: var(--primary); padding-left: 5px; }

            .social-links { display: flex; gap: 15px; margin-top: 20px; }
            .social-links a {
                width: 40px;
                height: 40px;
                background: rgba(255,255,255,0.05);
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                color: white;
                text-decoration: none;
            }
            .social-links a:hover { background: var(--primary); transform: translateY(-3px); }

            .footer-bottom {
                display: flex;
                justify-content: space-between;
                align-items: center;
                flex-wrap: wrap;
                gap: 20px;
                font-size: 0.9rem;
                color: #64748b;
            }

            /* Floating Share Button */
            .float-share {
                position: fixed;
                bottom: 30px;
                right: 30px;
                background: #25d366;
                color: white;
                padding: 15px 25px;
                border-radius: 50px;
                box-shadow: 0 10px 25px rgba(37, 211, 102, 0.4);
                text-decoration: none;
                font-weight: 700;
                display: flex;
                align-items: center;
                gap: 10px;
                z-index: 999;
                transition: 0.3s;
                animation: float 3s ease-in-out infinite;
            }

            .float-share:hover { transform: scale(1.1); background: #128c7e; }

            .btn-whatsapp {
                background: #25d366;
                color: white;
                padding: 12px 25px;
                border-radius: 15px;
                text-decoration: none;
                display: inline-flex;
                align-items: center;
                gap: 10px;
                font-weight: 600;
                margin-top: 15px;
                transition: 0.3s;
            }
            .btn-whatsapp:hover { background: #128c7e; }

            @media (max-width: 768px) {
                .navbar { padding: 1rem 5%; }
                .nav-links { display: none !important; } /* Hide links on mobile for simplicity */
                .search-card { flex-direction: column; height: auto; padding: 10px; border-radius: 18px; }
                .search-card input { padding: 15px; text-align: center; }
                .btn-action { padding: 18px; width: 100%; border-radius: 15px; }
                .hero { padding: 60px 15px; }
                .hero h1 { font-size: 2.2rem; }
                .hero p { font-size: 1rem; }
                
                .result-card {
                    flex-direction: column;
                    text-align: center;
                    padding: 20px;
                }
                .res-thumb { width: 100%; height: 200px; margin: 0 auto; }
                .res-info { width: 100%; }
                .res-title { font-size: 1.1rem; }
                
                .pricing { padding: 60px 5%; }
                .p-card.popular { transform: scale(1); } /* Disable scale on mobile to avoid layout breaking */
                .features { padding: 60px 5%; }
                
                .footer-grid { grid-template-columns: 1fr; text-align: center; gap: 30px; }
                .footer-col { display: flex; flex-direction: column; align-items: center; }
                .social-links { justify-content: center; }
                .footer-bottom { justify-content: center; text-align: center; }

                .float-share {
                    bottom: 20px;
                    right: 20px;
                    padding: 12px 20px;
                    font-size: 0.9rem;
                }
            }

            @media (max-width: 480px) {
                .hero h1 { font-size: 1.8rem; }
                .btn-download-final, .btn-copy-link { font-size: 0.95rem; }
                .modal-content { padding: 30px 20px; }
            }
                                <!-- Firebase SDKs -->
            <script src="https://www.gstatic.com/firebasejs/10.9.0/firebase-app-compat.js"></script>
            <script src="https://www.gstatic.com/firebasejs/10.9.0/firebase-auth-compat.js"></script>

        </head>
        <body style='cursor: none;'>
            <div class='cursor-point' id='cursor-point'></div>
            <div class='cursor-aura' id='cursor-aura'></div>
            <canvas id='particle-canvas'></canvas>
            <nav class='navbar'>
                <a class='logo' href='/'>
                    <i class="fab fa-instagram"></i> InstaStream <span>AI</span>
                </a>
                <div style="display: flex; gap: 15px; align-items: center;" class="nav-links">
                    <div class="nav-wallet" id="walletDisplay" onclick="updateCredits()" title="Points Balance">
                        <i class="fas fa-coins"></i>
                        <span id="navCredits">50</span>
                    </div>
                    <div class="nav-wallet nav-cash" onclick="openDashboard()" title="Earnings Dashboard">
                        <i class="fas fa-wallet"></i> ₹                        <span id="navBalance">0.00</span>
                    </div>
                    <div id="userProfile" style="display:none; align-items:center; gap:10px; font-weight:700; color:var(--dark);">
                        <img id="userAvatar" src="" style="width:35px; height:35px; border-radius:50%; object-fit:cover; border:2px solid var(--primary);"/>
                    </div>
                    <a class='btn-started' id='authBtn' href='javascript:void(0)' onclick="toggleAuth()">Login to Earn</a>
                </div>
            </nav>

            <!-- Earnings Dashboard Modal -->
            <div class="dashboard-modal" id="dashModal">
                <div class="dash-content">
                    <span class="dash-close" onclick="closeDashboard()">&times;</span>
                    <i class="fas fa-piggy-bank" style="font-size: 3rem; color: #22c55e;"></i>
                    <h2 style="margin-top: 15px;">Your Earnings</h2>
                    <div class="dash-balance">₹                        <span id="dashBalance">0.00</span>
                    </div>
                    <p style="color: #64748b; font-size: 0.9rem;">You earn ₹0.50 per download and ₹2.00 per referral!</p>

                    <div class="ref-box">
                        <input id="refUrl" readonly="" type="text" value="https://argha-7.blogspot.com?ref=..."/>
                        <button onclick="copyRef()" style="border:none; color:var(--primary); background:none; font-weight:700; cursor:pointer;">Copy</button>
                    </div>

                    <div style="text-align: left; margin: 20px 0;">
                        <label style="font-size:0.8rem; color:#64748b; font-weight:700;">Enter UPI ID for Withdrawal</label>
                        <input id="upiInput" placeholder="example@ybl" style="width:100%; padding: 12px; border-radius:10px; border:1px solid #e2e8f0; margin-top:5px;" type="text"/>
                    </div>
                    <button class="btn-withdraw" onclick="withdraw()">Withdraw to UPI</button>
                    <p style="font-size:0.7rem; color:#94a3b8; margin-top:15px;">Min. withdrawal: ₹50.00 | Processed in 24h</p>
                </div>
            </div>

            <main class='hero animate'>
                <!-- Top Ad Slot -->
                <div class='ad-slot'>
                    <!-- Paste Adsterra Header Ad Code Here -->
                    <span>ADSTERRA BANNER (728x90)</span>
                </div>

                <h1 class='animate delay-1' style='margin-top: 20px;'>Download <span id='typing-text'></span>
                    <span class='typing-cursor'>|</span>
                    <br/>
 Without Limits.</h1>
                <p class='animate delay-2'>The fastest, safest, and most intelligent Instagram downloader. Free forever, no watermark, works on any device.</p>

                <div class='limit-badge animate delay-2' id='limitBadge'>
                    <i class="fas fa-chart-line"></i>
                    <span id='limitText'>Downloads Left: 10 / 10</span>
                </div>

                <div class='search-card animate' style='animation-delay: 0.6s;'>
                    <input id='urlInput' placeholder='Paste Instagram URL here...' type='text'/>
                    <button class='btn-action' id='downloadBtn' onclick='startDownload()'>
                    Download Now
                    </button>
                </div>

                <!-- Middle Ad Slot -->
                <div class='ad-slot' style='margin-top:40px;'>
                    <span>ADSTERRA NATIVE AD AREA</span>
                </div>

                <div class='progress-card' id='progressUI'>
                    <div class="progress-header">
                        <img class="prog-thumb" id="progThumb" src="https://icons.iconarchive.com/icons/paomedia/small-n-flat/512/social-instagram-icon.png"/>
                        <div class="prog-info">
                            <div class="prog-title" id="progTitle">Analyzing video...</div>
                            <div id='statusText'>Initializing...</div>
                        </div>
                    </div>
                    <div class='progress-bar-wrap'>
                        <div id='progressBar'></div>
                    </div>
                    <div style="font-size: 0.85rem; color: #64748b;">This might take a few seconds using our backup servers.</div>
                </div>

                <div id='result'>
                    <div class='result-card'>
                        <img class='res-thumb' id='resThumb' src=''/>
                        <div class='res-info'>
                            <div class='res-title' id='resTitle'>Video Title</div>
                            <div class='res-meta' id='resMeta'>Ready for download</div>
                            <a class='btn-download-final' id='downloadLink' target='_blank'>
                                <i class='fas fa-download'></i> Download Video
                            </a>
                            <button class='btn-copy-link' onclick='copyLink()'>
                                <i class='far fa-clone'></i> Copy Link
                            </button>
                        </div>
                    </div>

                    <div class='success-banner'>
                        <i class='fas fa-check-circle'></i> Conversion completed successfully! Use the download button above.
                    </div>

                    <div style="margin-top: 30px; padding: 25px; background: rgba(37, 211, 102, 0.08); border-radius: 20px; border: 1px dashed #25D366; text-align: center;">
                        <h3 style="color:#065f46; margin-bottom:10px; font-size:1.2rem;">😍 Low on Points?</h3>
                        <p style="color:#1e293b; margin-bottom:20px;">Share this tool with friends on WhatsApp and get <b>+20 Bonus Points</b> instantly!</p>
                        <a href="https://api.whatsapp.com/send?text=Hey! Found this amazing tool to download Instagram Reels in HD for free! 🔥 Check here: https://argha-7.blogspot.com" class="btn-whatsapp" onclick="claimReward()" target="_blank">
                            <i class="fab fa-whatsapp"></i> Share & Earn +20
                        </a>
                    </div>
                </div>
            </main>

            <section class='features animate' id='features' style='background: linear-gradient(-45deg, #f8fafc, #eff6ff, #f8fafc); background-size: 400% 400%; animation: bgShift 15s ease infinite;'>
                <div class='f-card animate delay-1'>
                    <i class="fas fa-bolt"></i>
                    <h3>Turbo Speed</h3>
                    <p>Advanced multi-threaded technology ensures your downloads are finished in seconds, not minutes.</p>
                </div>
                <div class='f-card animate delay-2'>
                    <i class="fas fa-shield-halved"></i>
                    <h3>Privacy First</h3>
                    <p>We don't store your data or link history. Everything is processed and deleted instantly.</p>
                </div>
                <div class='f-card animate delay-1' style='animation-delay:0.6s;'>
                    <i class="fas fa-infinity"></i>
                    <h3>Smart Failover</h3>
                    <p>Exclusive AI technology that automatically switches servers if Instagram blocks the connection.</p>
                </div>
            </section>

            <!-- Pricing Section -->
            <section class='pricing animate' id='pricing'>
                <h2>Plans for Everyone</h2>
                <p>Choose the plan that fits your needs. Start for free today.</p>

                <div class='pricing-grid'>
                    <div class='p-card'>
                        <h4>Free Tier</h4>
                        <div class='price'>$0 <span>/month</span>
                        </div>
                        <ul>
                            <li>
                                <i class="fas fa-check"></i> 10 Daily Downloads</li>
                            <li>
                                <i class="fas fa-check"></i> HD Quality Video</li>
                            <li>
                                <i class="fas fa-check"></i> Smart Failover System</li>
                            <li>
                                <i class="fas fa-check"></i> No Registration</li>
                        </ul>
                        <a href="#" class="btn-outline">Current Plan</a>
                    </div>

                    <div class='p-card popular'>
                        <div style="background:var(--primary); color:white; font-size:0.7rem; padding:5px 15px; position:absolute; top:20px; right:-30px; transform:rotate(45deg); width:120px;">FULL LICENSE</div>
                        <h4>Theme License</h4>
                        <div class='price'>$29 <span>/lifetime</span>
                        </div>
                        <ul>
                            <li>
                                <i class="fas fa-check"></i> Own this Blogger Theme</li>
                            <li>
                                <i class="fas fa-check"></i> 1 Year Script Updates</li>
                            <li>
                                <i class="fas fa-check"></i> Setup Documentation</li>
                            <li>
                                <i class="fas fa-check"></i> Removal of Credits</li>
                        </ul>
                        <a href="#" class="btn-started" style="display:block;">Buy Theme Now</a>
                    </div>

                    <div class='p-card'>
                        <h4>Pro Script</h4>
                        <div class='price'>$10 <span>/year</span>
                        </div>
                        <ul>
                            <li>
                                <i class="fas fa-check"></i> Private API Access</li>
                            <li>
                                <i class="fas fa-check"></i> No IP Blocking</li>
                            <li>
                                <i class="fas fa-check"></i> Custom Domain Support</li>
                            <li>
                                <i class="fas fa-check"></i> Priority Email Support</li>
                        </ul>
                        <a href="#" class="btn-outline">Subscribe Now</a>
                    </div>
                </div>
            </section>

            <!-- Sign Up Modal -->
            <div class="modal" id="signupModal">
                <div class="modal-content">
                    <i class="fas fa-user-plus" style="font-size:3rem; color:var(--primary); margin-bottom:20px;"></i>
                    <h2>Unlock More!</h2>
                    <p style="color:#64748b; margin-bottom:30px;">Sign up today and get 10 extra daily downloads for free!</p>
                    <input id="signupEmail" placeholder="Enter your email" style="width:100%; padding:15px; border-radius:12px; border:1px solid #ddd; margin-bottom:15px;" type="email"/>
                    <button class="btn-started" onclick="simulatedSignup()" style="width:100%;">Create Account</button>
                    <p onclick="closeModal()" style="margin-top:20px; font-size:0.8rem; cursor:pointer; color:#94a3b8;">Maybe later</p>
                </div>
            </div>

            <!-- Bottom Ad Slot -->
            <div class='ad-slot' style='margin: 50px auto;'>
                <span>ADSTERRA FOOTER AD</span>
            </div>

            <footer>
                <div class='footer-grid'>
                    <div class='footer-col'>
                        <a class='logo' href='/' style='color:white; margin-bottom:20px; display:inline-block; font-size:1.4rem;'>
                            <i class="fab fa-instagram"></i> InstaStream
                        </a>
                        <p style='color: #94a3b8; font-size: 0.9rem; line-height: 1.6;'>The world's most intelligent Instagram video downloader. Fast, free, and secure solution for everyone.</p>
                        <div class="social-links">
                            <a href="#">
                                <i class="fab fa-facebook-f"></i>
                            </a>
                            <a href="#">
                                <i class="fab fa-twitter"></i>
                            </a>
                            <a href="#">
                                <i class="fab fa-instagram"></i>
                            </a>
                            <a href="#">
                                <i class="fab fa-linkedin-in"></i>
                            </a>
                        </div>
                    </div>

                    <div class='footer-col'>
                        <h4>Product</h4>
                        <ul>
                            <li>
                                <a href="#">Reels Downloader</a>
                            </li>
                            <li>
                                <a href="#">Video Downloader</a>
                            </li>
                            <li>
                                <a href="#">Photo Downloader</a>
                            </li>
                            <li>
                                <a href="#">MP3 Converter</a>
                            </li>
                        </ul>
                    </div>

                    <div class='footer-col'>
                        <h4>Support</h4>
                        <ul>
                            <li>
                                <a href="#pricing">Pricing</a>
                            </li>
                            <li>
                                <a href="#">Help Center</a>
                            </li>
                            <li>
                                <a href="#">Contact Us</a>
                            </li>
                            <li>
                                <a href="#">Buy a Coffee</a>
                            </li>
                        </ul>
                    </div>

                    <div class='footer-col'>
                        <h4>Legal</h4>
                        <ul>
                            <li>
                                <a href="#">Privacy Policy</a>
                            </li>
                            <li>
                                <a href="#">Terms of Service</a>
                            </li>
                            <li>
                                <a href="#">Cookie Policy</a>
                            </li>
                            <li>
                                <a href="#">DMCA Notice</a>
                            </li>
                        </ul>
                    </div>
                </div>

                <div class='footer-bottom'>
                    <p>&copy; 2024 InstaStream Pro AI. Built for the community.</p>
                    <div style="display:flex; gap:20px;">
                        <a href="#" style="color:#64748b; text-decoration:none;">Privacy</a>
                        <a href="#" style="color:#64748b; text-decoration:none;">Terms</a>
                        <a href="#" style="color:#64748b; text-decoration:none;">Cookies</a>
                    </div>
                </div>
            </footer>

            <a href="https://api.whatsapp.com/send?text=Check out this awesome Instagram Downloader!" class="float-share" target="_blank">
                <i class="fab fa-whatsapp"></i> Tell a Friend
            </a>

            <script>
            // Hiding API and Secrets from simple inspection
            const _0x1a2b = ["aHR0cHM6Ly9hcmdoYS03LWluc3RhLWRvd25sb2FkZXItd2ViLmhmLnNwYWNl", "aW5zdGFfcHJvX2FpX3NlY3VyZV85OQ=="];
            const API_BASE = atob(_0x1a2b[0]);
            const _S = atob(_0x1a2b[1]);

            // Initialize Firebase
            const firebaseConfig = {
              apiKey: "AIzaSyA7SyWG2xwjGnA9wcKeQmqLwzeZ2J1q09w",
              authDomain: "instastream-29bd8.firebaseapp.com",
              projectId: "instastream-29bd8",
              storageBucket: "instastream-29bd8.firebasestorage.app",
              messagingSenderId: "245735914604",
              appId: "1:245735914604:web:07e74c838ad2f895aaec49",
              measurementId: "G-KVQTQC8E00"
            };
            if (!firebase.apps.length) {
                firebase.initializeApp(firebaseConfig);
            }
            const auth = firebase.auth();
            const provider = new firebase.auth.GoogleAuthProvider();

            // Setup Auth State Listener
            auth.onAuthStateChanged(user => {
                if (user) {
                    document.getElementById('authBtn').innerText = 'Logout';
                    document.getElementById('userProfile').style.display = 'flex';
                    document.getElementById('userAvatar').src = user.photoURL || "https://icons.iconarchive.com/icons/paomedia/small-n-flat/512/user-male-icon.png";
                    updateCredits(); // Fetch balances right after login
                } else {
                    document.getElementById('authBtn').innerText = 'Login to Earn';
                    document.getElementById('userProfile').style.display = 'none';
                    document.getElementById('navCredits').innerText = '0';
                    document.getElementById('navBalance').innerText = '0.00';
                }
            });

            async function toggleAuth() { // Login/Logout action
                const user = auth.currentUser;
                if(user) {
                    await auth.signOut();
                    alert("Logged out successfully.");
                } else {
                    try {
                        await auth.signInWithPopup(provider);
                    } catch(e) {
                        console.log("Login failed:", e);
                    }
                }
            }

            async function getSecureHeaders() {
                let token = "";
                if (auth.currentUser) {
                    token = await auth.currentUser.getIdToken();
                }
                return { 
                    'Content-Type': 'application/json',
                    'X-App-Secret': _S,
                    'Authorization': `Bearer ${token}`
                };
            }

            let currentMetadata = {};

            async function startDownload() {
                const url = document.getElementById('urlInput').value.trim();
                const btn = document.getElementById('downloadBtn');
                const progressUI = document.getElementById('progressUI');
                const statusText = document.getElementById('statusText');
                const progressBar = document.getElementById('progressBar');
                const resultDiv = document.getElementById('result');

                if (!url) {
                    alert('Please paste a link first!');
                    return;
                }

                // Reset and Show Progress
                btn.disabled = true;
                btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Initializing...';
                resultDiv.style.display = 'none';
                progressUI.style.display = 'flex';
                progressBar.style.width = '10%';
                statusText.innerText = 'Connecting to server...';

                // 1. Fetch Instant Preview (Wow Factor)
                try {
                    const hdrs = await getSecureHeaders();
                    const prevRes = await fetch(`${API_BASE}/preview`, {
                        method: 'POST',
                        headers: hdrs,
                        body: JSON.stringify({ url })
                    });
                    if (prevRes.ok) {
                        const prevData = await prevRes.json();
                        if (prevData && prevData.success) {
                            currentMetadata = prevData;
                            document.getElementById('progThumb').src = prevData.thumbnail;
                            document.getElementById('progTitle').innerText = prevData.title;
                        }
                    }
                } catch (e) {
                    console.log("Preview failed:", e);
                }

                // 2. Start Actual Download
                try {
                    const hdrs = await getSecureHeaders();
                    const response = await fetch(`${API_BASE}/download`, {
                        method: 'POST',
                        headers: hdrs,
                        body: JSON.stringify({ url: url })
                    });
                    const data = await response.json();

                    if (data.success) {
                        if (data.status === 'ready') {
                            showSuccess(data);
                            // Confetti burst on success! 🎉
                            confetti({
                                particleCount: 150,
                                spread: 70,
                                origin: { y: 0.6 },
                                colors: ['#3b82f6', '#2dd4bf', '#f09433', '#e6683c']
                            });
                        } else if (data.status === 'pending') {
                            statusText.innerText = 'Server blocked. Activating Smart Backup...';
                            progressBar.style.width = '50%';
                            pollStatus(data.job_id);
                        }
                    } else {
                        showError(data.message || 'Error occurred');
                    }
                } catch (e) {
                    showError('Connection Error. Check console.');
                    console.error("Download Error:", e);
                }
            }

            async function pollStatus(jobId) {
                const statusText = document.getElementById('statusText');
                const progressBar = document.getElementById('progressBar');
                let count = 0;

                const interval = setInterval(async () => {
                    count++;
                    if (count > 60) { clearInterval(interval); showError('Process Timeout.'); return; }

                    try {
                        const res = await fetch(`${API_BASE}/status/${jobId}`);
                        const data = await res.json();

                        if (data.status === 'ready') {
                            clearInterval(interval);
                            showSuccess({
                                filename: data.filename,
                                title: currentMetadata.title || "Instagram Video",
                                thumbnail: currentMetadata.thumbnail || "https://icons.iconarchive.com/icons/paomedia/small-n-flat/512/social-instagram-icon.png"
                            });
                        } else {
                            statusText.innerText = `Video Processing... (${count * 5}s)`;
                            progressBar.style.width = `${50 + (count * 0.7)}%`;
                        }
                    } catch (e) {}
                }, 5000);
            }

            function showSuccess(data) {
                document.getElementById('progressUI').style.display = 'none';
                const res = document.getElementById('result');
                res.style.display = 'block';
                
                document.getElementById('resTitle').innerText = data.title || "Instagram Video";
                document.getElementById('resThumb').src = data.thumbnail || "https://icons.iconarchive.com/icons/paomedia/small-n-flat/512/social-instagram-icon.png";
                document.getElementById('downloadLink').href = `${API_BASE}/files/${data.filename}`;
                
                // Refresh limits
                if(typeof updateLimits === 'function') updateLimits();
                
                resetBtn();
            }

            function copyLink() {
                const url = document.getElementById('urlInput').value;
                navigator.clipboard.writeText(url).then(() => {
                    alert('Link copied to clipboard!');
                });
            }

            function showError(msg) {
                document.getElementById('statusText').innerText = msg;
                document.getElementById('statusText').style.color = '#ef4444';
                resetBtn();
            }

            function resetBtn() {
                const btn = document.getElementById('downloadBtn');
                btn.disabled = false;
                btn.innerText = 'Download Now';
            }

            let mouseX = window.innerWidth / 2;
            let mouseY = window.innerHeight / 2;
            let auraX = mouseX, auraY = mouseY;

            // Limit & Points & referral Logic
            let referralID = "";
            
            // Capture 'ref' from URL
            const urlParams = new URLSearchParams(window.location.search);
            const refParam = urlParams.get('ref') || "";

            async function updateCredits() {
                try {
                    const hdrs = await getSecureHeaders();
                    const res = await fetch(`${API_BASE}/check-limit`, {
                        method: 'POST',
                        headers: hdrs,
                        body: JSON.stringify({ ref: refParam })
                    });
                    const data = await res.json();
                    
                    if (!data.success === false && data.message && data.message.includes('Unauthorized')) {
                        // User not logged in, silented fallback handled by UI display
                        return;
                    }

                    const credits = data.credits || 0;
                    const balance = data.balance || 0;
                    referralID = data.referral_id || "";

                    // limitText does not exist anymore, removing to prevent console errors
                    const navCre = document.getElementById('navCredits');
                    if (navCre) navCre.innerText = credits;
                    const navBal = document.getElementById('navBalance');
                    if (navBal) navBal.innerText = balance.toFixed(2);
                    const dashBal = document.getElementById('dashBalance');
                    if (dashBal) dashBal.innerText = balance.toFixed(2);
                    const refInp = document.getElementById('refUrl');
                    if (refInp && referralID) refInp.value = `https://argha-7.blogspot.com?ref=${referralID}`;
                    
                    const wd = document.getElementById('walletDisplay');
                    if (wd) {
                        if (credits < 10) {
                            wd.style.background = 'rgba(239, 68, 68, 0.1)';
                        } else {
                            wd.style.background = 'rgba(59, 130, 246, 0.1)';
                        }
                    }
                } catch (e) {
                    console.error("Credit fetch failed");
                }
            }

            function openDashboard() { 
                if(!auth.currentUser) {
                    alert("Please Login with Google to view your earnings!");
                    return;
                }
                document.getElementById('dashModal').style.display = 'flex'; 
            }
            function closeDashboard() { document.getElementById('dashModal').style.display = 'none'; }
            function copyRef() {
                const copyText = document.getElementById("refUrl");
                copyText.select();
                document.execCommand("copy");
                alert("Referral link copied!");
            }

            async function withdraw() {
                const upi = document.getElementById('upiInput').value;
                if (!upi.includes('@')) { alert('Please enter valid UPI ID'); return; }
                
                try {
                    const hdrs = await getSecureHeaders();
                    const res = await fetch(`${API_BASE}/withdraw`, {
                        method: 'POST',
                        headers: hdrs,
                        body: JSON.stringify({ upi_id: upi })
                    });
                    const data = await res.json();
                    alert(data.message);
                } catch (e) {
                    alert('Withdrawal failed. Try again.');
                }
            }

            async function claimReward() {
                // Short delay to ensure they click the link
                setTimeout(async () => {
                    if (!auth.currentUser) return; // Must be logged in to earn
                    try {
                        const hdrs = await getSecureHeaders();
                        const res = await fetch(`${API_BASE}/reward-share`, {
                            method: 'POST',
                            headers: hdrs,
                            body: JSON.stringify({})
                        });
                        const data = await res.json();
                        if (data.success) {
                            updateCredits();
                            if(typeof confetti !== "undefined") {
                                confetti({ particleCount: 150, spread: 70, origin: { y: 0.6 } });
                            }
                            alert(data.message);
                        }
                    } catch (e) {
                        console.error("Reward failed");
                    }
                }, 2000);
            }


            updateCredits();

            // --- INTERACTIVE FEATURES: TYPING & PARTICLES ---

            // 1. Typing Effect Logic
            const typingText = document.getElementById('typing-text');
            const phrases = ["Reels", "Stories", "IGTV", "Videos", "Photos"];
            let phraseIdx = 0;
            let charIdx = 0;
            let isDeleting = false;

            function typeEffect() {
                const currentPhrase = phrases[phraseIdx];
                if (isDeleting) {
                    typingText.innerText = currentPhrase.substring(0, charIdx - 1);
                    charIdx--;
                } else {
                    typingText.innerText = currentPhrase.substring(0, charIdx + 1);
                    charIdx++;
                }

                let speed = isDeleting ? 50 : 150;
                if (!isDeleting && charIdx === currentPhrase.length) {
                    isDeleting = true;
                    speed = 2000; // Pause at end
                } else if (isDeleting && charIdx === 0) {
                    isDeleting = false;
                    phraseIdx = (phraseIdx + 1) % phrases.length;
                    speed = 500;
                }
                setTimeout(typeEffect, speed);
            }
            typeEffect();

            // 2. Antigravity Halo Particle System
            const canvas = document.getElementById('particle-canvas');
            const ctx = canvas.getContext('2d');
            let particles = [];
            // Signature Antigravity Galaxy Palette
            const colors = ['#3b82f6', '#2dd4bf', '#f09433', '#cc2366', '#dc2743'];

            function resize() {
                canvas.width = window.innerWidth;
                canvas.height = window.innerHeight;
            }
            window.addEventListener('resize', resize);
            resize();

            class Particle {
                constructor() {
                    this.init();
                }
                init() {
                    // Full screen distribution with central weight
                    this.x = Math.random() * canvas.width;
                    this.y = Math.random() * canvas.height;
                    
                    // Base position for return logic (centered-weight random)
                    this.baseX = this.x;
                    this.baseY = this.y;
                    
                    this.size = Math.random() * 2 + 0.3; 
                    this.color = colors[Math.floor(Math.random() * colors.length)];
                    this.density = (Math.random() * 30) + 15;
                    this.alpha = Math.random() * 0.4 + 0.1;

                    this.vX = (Math.random() - 0.5) * 0.8;
                    this.vY = (Math.random() - 0.5) * 0.8;
                    
                    // Transformation Properties
                    this.stretch = 1;
                    this.angle = 0;
                    this.isInner = false;
                }
                update() {
                    // 1. Constant Jitter & Drift
                    this.baseX += this.vX;
                    this.baseY += this.vY;
                    if (this.baseX < 0) this.baseX = canvas.width;
                    if (this.baseX > canvas.width) this.baseX = 0;
                    if (this.baseY < 0) this.baseY = canvas.height;
                    if (this.baseY > canvas.height) this.baseY = 0;

                    // 2. Interaction & Stretch Logic
                    let dx = mouseX - this.x;
                    let dy = mouseY - this.y;
                    let distance = Math.sqrt(dx*dx + dy*dy);
                    let maxDistance = 200;

                    // Color Shift Logic (Inner = Black)
                    this.isInner = distance < 80;

                    if (distance < maxDistance) {
                        let force = (maxDistance - distance) / maxDistance;
                        let directionX = (dx / distance) * force * this.density;
                        let directionY = (dy / distance) * force * this.density;
                        
                        this.x -= directionX;
                        this.y -= directionY;
                        
                        // Stretch & Compress Logic
                        this.angle = Math.atan2(dy, dx);
                        this.stretch = 1 + (force * 2.5); // Elongate based on force
                    } else {
                        this.x -= (this.x - this.baseX) / 10;
                        this.y -= (this.y - this.baseY) / 10;
                        this.stretch = 1; // Return to circle
                    }
                }
                draw() {
                    ctx.save();
                    ctx.translate(this.x, this.y);
                    ctx.rotate(this.angle);
                    
                    // Draw Color based on distance
                    ctx.fillStyle = this.isInner ? "#000000" : this.color;
                    ctx.globalAlpha = this.isInner ? 0.9 : this.alpha;
                    
                    ctx.beginPath();
                    // Stretch along X axis
                    ctx.ellipse(0, 0, this.size * this.stretch, this.size / Math.sqrt(this.stretch), 0, 0, Math.PI * 2);
                    ctx.fill();
                    
                    ctx.restore();
                    ctx.globalAlpha = 1;
                }
            }

            function initParticles() {
                particles = [];
                // Ultra-dense particle field (1 dot per 800 pixels)
                const count = (canvas.width * canvas.height) / 800;
                for (let i = 0; i < count; i++) {
                    particles.push(new Particle());
                }
            }
            initParticles();
            window.addEventListener('resize', initParticles);

            function animate() {
                ctx.clearRect(0, 0, canvas.width, canvas.height);
                for (let i = 0; i < particles.length; i++) {
                    particles[i].update();
                    particles[i].draw();
                }
                requestAnimationFrame(animate);
            }
            animate();

            // 3. Aura Cursor Easing Logic
            const point = document.getElementById('cursor-point');
            const aura = document.getElementById('cursor-aura');
            document.addEventListener('mousemove', (e) => {
                mouseX = e.clientX;
                mouseY = e.clientY;
                if(point) point.style.transform = `translate(${mouseX}px, ${mouseY}px)`;
            });

            function updateAura() {
                // Smooth easing (lerp)
                auraX += (mouseX - auraX) * 0.15;
                auraY += (mouseY - auraY) * 0.15;
                aura.style.left = auraX + 'px';
                aura.style.top = auraY + 'px';
                requestAnimationFrame(updateAura);
            }
            updateAura();

            // Hover interactions
            const interactives = document.querySelectorAll('button, a, input, .f-card, .p-card');
            interactives.forEach(el => {
                el.addEventListener('mouseenter', () => aura.classList.add('hover'));
                el.addEventListener('mouseleave', () => aura.classList.remove('hover'));
            });
        </script>
</body>
</html>
